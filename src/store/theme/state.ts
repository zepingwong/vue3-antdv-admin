import { IThemeConfig } from "@/types/IThemeConfig"

const state: IThemeConfig = {
    layout: "vertical",
    device: "desktop",
    collapsed: false,
    showTabs: true,
    tabsBarStyle: "smooth"
}

export default state
